/**
 * Properties available for the ItemsPager component
 */
 interface ButtonProps{
    title: string;
    showButton: boolean;
    buttonLabel?: string;
    onButtonClick?: ()=>void;
}

export default ButtonProps;
