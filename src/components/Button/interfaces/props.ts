/**
 * Properties available for the Button component
 */
interface ButtonProps{
    color?: string;
    background?: string;
    children: string;
    onClick?: () => void;
}

export default ButtonProps;
